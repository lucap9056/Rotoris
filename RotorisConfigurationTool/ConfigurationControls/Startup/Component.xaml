<UserControl x:Class="RotorisConfigurationTool.ConfigurationControls.Startup.Component"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RotorisConfigurationTool.ConfigurationControls.Startup"
             xmlns:i18n="clr-namespace:RotorisConfigurationTool.Properties.ConfigurationControls.Startup"
             mc:Ignorable="d" 
             Width="200"
             >
    <Grid Height="24" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock 
            Text="{x:Static i18n:I18n.RunOnStartup}" 
            Grid.Column="0"
            VerticalAlignment="Center"
            Margin="0,0,10,0"
            />

        <ToggleButton IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=StartupEnabled}"
                      Grid.Column="1"
                      Width="48" Height="24"
                      HorizontalAlignment="Left"
                      >
            <ToggleButton.Template>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid>
                        <Border x:Name="Border" 
                                    CornerRadius="12" 
                                    BorderBrush="#AAAAAA" 
                                    BorderThickness="1" />

                        <Ellipse x:Name="Thumb" 
                                     Width="20" Height="20" 
                                     Fill="White" 
                                     Stroke="#888888" 
                                     StrokeThickness="1" 
                                     HorizontalAlignment="Left" 
                                     Margin="2"
                                     RenderTransformOrigin="0.5,0.5">
                            <Ellipse.RenderTransform>
                                <TranslateTransform x:Name="Translate" X="0"/>
                            </Ellipse.RenderTransform>
                        </Ellipse>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#4CAF50"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Translate" 
                                                             Storyboard.TargetProperty="X" 
                                                             From="0" To="24" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Translate" 
                                                             Storyboard.TargetProperty="X" 
                                                             From="24" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="Border" Property="Background" Value="#CCCCCC"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>
    </Grid>
</UserControl>
