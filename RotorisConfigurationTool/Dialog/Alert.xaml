<Window x:Class="RotorisConfigurationTool.Dialog.Alert"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RotorisConfigurationTool.Dialog"
        xmlns:i18n="clr-namespace:RotorisConfigurationTool.Properties.Dialog.Alert"
        mc:Ignorable="d"
        Title=""
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        SizeToContent="WidthAndHeight"
        >
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock x:Name="MessageText"
                       TextWrapping="Wrap"
                       Text="Text"
                       FontSize="14"
                       MaxWidth="400"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">

            <Button x:Name="OkButton" 
                    Content="{x:Static i18n:I18n.Ok}" 
                    Padding="15,5" 
                    Margin="5,0" 
                    IsDefault="True"
                    Click="Button_Click"
                    Visibility="Collapsed"
                    />

            <Button x:Name="CancelButton" 
                    Content="{x:Static i18n:I18n.Cancel}" 
                    Padding="15,5" 
                    Margin="5,0"
                    IsCancel="True"
                    Click="Button_Click"
                    Visibility="Collapsed"
                    />

            <Button x:Name="YesButton" 
                    Content="{x:Static i18n:I18n.Yes}" 
                    Padding="15,5" 
                    Margin="5,0"
                    Click="Button_Click"
                    Visibility="Collapsed"
                    />

            <Button x:Name="NoButton" 
                    Content="{x:Static i18n:I18n.No}" 
                    Padding="15,5" 
                    Margin="5,0"
                    Click="Button_Click"
                    Visibility="Collapsed"
                    />

        </StackPanel>
    </Grid>
</Window>
