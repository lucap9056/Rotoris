<Window x:Class="RotorisConfigurationTool.Dialog.SizeSlider.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RotorisConfigurationTool.Dialog.SizeSlider"
        xmlns:i18n="clr-namespace:RotorisConfigurationTool.Properties.Dialog.SizeSlider"
        mc:Ignorable="d"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="" Height="140" Width="200"
        x:Name="SizeSildeWindow"
        >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="{x:Static i18n:I18n.Size}" 
                   Grid.Column="0" 
                   Margin="0,5,10,5" 
                   VerticalAlignment="Center"/>

            <Slider 
                Grid.Column="1" 
                x:Name="slider"
                Minimum="200" 
                Maximum="1200" 
                Value="{Binding ElementName=SizeSildeWindow, Path=UiSize}"
                IsSnapToTickEnabled="True" 
                TickFrequency="10"
                HorizontalAlignment="Stretch" 
                Margin="0,0,0,0"/>
        </Grid>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">

            <Button Content="{x:Static i18n:I18n.Confirm}" 
                    IsDefault="True" 
                    Width="75" 
                    Margin="0,0,10,0"
                    Click="ConfirmButton_Click"
                    />

            <Button Content="{x:Static i18n:I18n.Cancel}" 
                    IsCancel="True" 
                    Width="75" 
                    Click="CancelButton_Click"
                    />
        </StackPanel>
    </Grid>
</Window>
